using UnityEngine;

// Marker component put on every instantiated eye (see CreatureEyeAttacher), so
// the eyes can be found and shown/hidden later without guessing by name.
public class CreatureEye : MonoBehaviour
{
}
